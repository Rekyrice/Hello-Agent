---
description: 在任务收尾时将长对话压缩为可复用的关键节点与状态说明，降低后续上下文 token 消耗。
globs:
  - "**/*"
alwaysApply: false
---

# Session Compaction（会话压缩规范）

目标：优先进行 Agent 内部上下文压缩（减少后续 token 消耗），并在需要时将压缩结果转成可执行状态供下一次续做。

## 0) 首要目标与执行优先级（强约束）
- 每次触发本规则时，**必须先完成 Agent 内部压缩**，再决定是否输出或落盘文档。
- 内部压缩为首要目标；`docs/session/*.md` 仅为可选产物，不是默认必做项。
- 若用户未明确要求“生成/更新会话压缩文件”，默认仅执行内部压缩，不写文件。
- 若用户明确要求落盘，再按本规范结构输出到文档。

## 1) 适用时机
- 任务阶段结束（如：环境打通、功能完成、仓库已推送）。
- 对话已很长，后续需要节省 token。
- 准备切换任务，但希望保留可追溯状态。

## 2) 压缩输出结构（必须）
- `背景目标`：用户想解决什么（1-2 行）。
- `关键节点`：按时间顺序列 5-10 条重要动作。
- `当前状态`：环境、依赖、仓库、配置的最终事实状态。
- `风险与待办`：未完成项、潜在风险、下一步建议。
- `可直接复用命令`：最常用的 3-8 条命令。

> 说明：上述结构既可用于对话内简版输出，也可用于落盘文档；但无用户明确要求时不强制落盘。

## 3) 状态字段（建议）
- Python 解释器与版本
- 关键依赖版本
- `.env` / `.env.example` / `.gitignore` 状态
- Git 远程、分支、标签、是否已推送
- 运行验证结果（成功/失败与关键输出）

## 4) 产物位置
- 推荐存放：`docs/session/`
- 文件名建议：`YYYY-MM-DD_会话压缩.md`
- 仅在用户明确要求“写入文件”时使用本节。

## 5) 约束
- 不写入任何密钥明文。
- 不复述无关聊天内容。
- 结论必须可执行、可验证。
- 不得将“生成压缩文档”误当作首要目标，首要目标始终是内部上下文压缩。
